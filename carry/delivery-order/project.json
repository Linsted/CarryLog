{
  "name": "delivery-order",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "delivery-order/src",
  "projectType": "application",
  "tags": [],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "webpack-cli build",
        "args": ["node-env=production"]
      },
      "configurations": {
        "development": {
          "args": ["node-env=development"]
        }
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "dependsOn": ["build"],
      "options": {
        "buildTarget": "delivery-order:build",
        "runBuildTargetDependencies": false
      },
      "configurations": {
        "development": {
          "buildTarget": "delivery-order:build:development"
        },
        "production": {
          "buildTarget": "delivery-order:build:production"
        }
      }
    }
  }
}
